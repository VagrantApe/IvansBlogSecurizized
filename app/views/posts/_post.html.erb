<%= content_tag :section, class: "post #{post.publish_status}" do %>
  <h2>Post <%= sanitize "#{post.id}: #{post.title}" %></h2>
  <div class="post"><%= sanitize post.body %></div>
  <%= render 'post_nav', post: post %>
  <%#= render post.comments %>
<% end %>
